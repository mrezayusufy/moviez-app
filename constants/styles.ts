import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageBg: {
    width: "100%",
    position: "absolute",
    zIndex: 0,
    top: 0,
  },
  logo: {
    width: 80,
    height: 80,
    marginTop: 80,
    marginBottom: 20,
    marginInline: "auto",
  }
})